name: Basic CI Check
 
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
 
jobs:
  check:
    runs-on: ubuntu-latest
 
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
 
      - name: Simple confirmation step
        run: echo "✅ GitHub Actions is working successfully!"
 
